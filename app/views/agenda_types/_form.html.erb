<%= form_for(@agenda_type) do |f| %>
  <% if @agenda_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agenda_type.errors.count, "error") %> prohibited this agenda_type from being saved:</h2>

      <ul>
      <% @agenda_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :typestring %><br />
    <%= f.text_field :typestring %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
